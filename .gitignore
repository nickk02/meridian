node_modules/
dist/
.wrangler/
.dev.vars
*.log
.DS_Store

# Local-only verification tooling and screenshots (Playwright via installed Edge)
.local-shot-*.png
/scripts/

# Cloudflare API token, never committed
*.cf-token
.cf-token

# Claude / agent artifacts, never committed
.remember/
.claude/
CLAUDE.md
AGENTS.md
