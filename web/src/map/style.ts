// Keyless dark VECTOR basemap: CARTO dark-matter-gl style (OSM data, CARTO
// styling). Vector, not raster, so coastlines and labels stay crisp at every
// zoom and on the globe instead of blurring. No token required; CARTO/OSM
// attribution rides in the style itself. MapLibre accepts a style URL directly,
// and our data layers are added on top in the map's load handler.
export const darkBasemap = "https://basemaps.cartocdn.com/gl/dark-matter-gl-style/style.json";
